// Persona-v15 deprecated and removed.
// Replacing with a stub that throws if invoked so callers fail-fast during migration.
export function buildOmegaV15GovernedPrompt() {
  throw new Error('buildOmegaV15GovernedPrompt has been removed. Use FORGE v2 via computeForge and governance-loader.js instead.');
}